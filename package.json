{
  "name": "Slack-Outgoing-Webhook",
  "version": "1.0.0",
  "description": "Vanilla implementation of an outgoing webhook for slack use",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "dependencies": {
    "body-parser": "^1.13.3",
    "express": "^4.13.3"
  }
}